# Local Development (uses Vite proxy defined in vite.config.ts)
# No VITE_API_URL needed - defaults to /api which proxies to http://127.0.0.1:5000

# Production/Deployment (e.g., Vercel)
# Replace with your actual deployed backend URL
VITE_API_URL=https://your-backend-url.com
